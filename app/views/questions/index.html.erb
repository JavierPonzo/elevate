<div class="container-fluid chatbot mb-5 mt-5">
  <h1 class="hero-title">Chatea con DrBot</h1>
  <div id="questions">
    <%= render @questions %>
  </div>

  <%= render "form", question: @question %>
</div>
